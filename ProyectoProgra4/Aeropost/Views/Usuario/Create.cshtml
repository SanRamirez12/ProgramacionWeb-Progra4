@model Aeropost.Models.Usuario
@{
    ViewData["Title"] = "Crear Usuario";
}

<h1>Crear Usuario</h1>
<hr />

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <!-- DIAGNÓSTICO: imprime todos los errores del ModelState -->
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-warning">
                    <strong>Diagnóstico ModelState:</strong>
                    <ul>
                        @foreach (var kvp in ViewData.ModelState)
                        {
                            foreach (var err in kvp.Value.Errors)
                            {
                                <li><code>@kvp.Key</code>: @err.ErrorMessage</li>
                            }
                        }
                    </ul>
                </div>
            }


            <div class="form-group">
                <label asp-for="Nombre"></label>
                <input asp-for="Nombre" class="form-control" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Cedula"></label>
                <input asp-for="Cedula" class="form-control" type="text" />
                <span asp-validation-for="Cedula" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>Género</label><br />
                <input type="radio" asp-for="Genero" value="Masculino" /> Masculino
                <input type="radio" asp-for="Genero" value="Femenino" /> Femenino
                <span asp-validation-for="Genero" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="FechaRegistro"></label>
                <input asp-for="FechaRegistro" class="form-control" type="date" />
                <span asp-validation-for="FechaRegistro" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>Estado</label><br />
                <input type="radio" asp-for="Estado" value="Activo" /> Activo
                <input type="radio" asp-for="Estado" value="Inactivo" /> Inactivo
                <span asp-validation-for="Estado" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Correo"></label>
                <input asp-for="Correo" class="form-control" type="email" />
                <span asp-validation-for="Correo" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Username"></label>
                <input asp-for="Username" class="form-control" />
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Password"></label>
                <input asp-for="Password" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

<<<<<<< HEAD
            <div class="form-group">
                <label asp-for="ConfirmPassword"></label>
                <input asp-for="ConfirmPassword" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
=======
            <!-- Confirm Password (no está en el modelo) -->
            <div class="form-group">
                <label for="ConfirmPassword" class="control-label">Confirmar Contraseña</label>
                <input id="ConfirmPassword" name="ConfirmPassword" class="form-control"
                       type="password" required placeholder="Repita la contraseña" />
                <span class="text-danger">
                    @ViewData.ModelState["ConfirmPassword"]?.Errors.FirstOrDefault()?.ErrorMessage
                </span>
>>>>>>> 7dc41665555ab4171dc0ac84cc0f691c46493418
            </div>

            <div class="form-group mt-3">
                <input type="submit" value="Crear" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                <button type="reset" class="btn btn-light">Limpiar</button>
            </div>
        </form>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index">Volver a la Lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // Prevalidación en cliente (evita post si no coincide)
        (function () {
          const form = document.querySelector('form[asp-action="Create"]');
          if (!form) return;
          const pwd = form.querySelector('input[name="Password"]');
          const cpw = form.querySelector('input[name="ConfirmPassword"]');
          const summary = form.querySelector('[asp-validation-summary]');

          form.addEventListener('submit', function (e) {
            if (!pwd || !cpw) return;
            if (summary) summary.innerHTML = '';
            const p = pwd.value || '', c = cpw.value || '';
            const errs = [];
            if (p.length < 8) errs.push('La contraseña debe tener al menos 8 caracteres.');
            if (p !== c) errs.push('Las contraseñas no coinciden.');
            if (errs.length) {
              e.preventDefault();
              if (summary) {
                const ul = document.createElement('ul');
                errs.forEach(m => { const li = document.createElement('li'); li.textContent = m; ul.appendChild(li); });
                summary.appendChild(ul);
              }
            }
          });
        })();
    </script>
}



